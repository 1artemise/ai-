# import os
# import json
#
# # 要读取的JSON文件名
# json_file_name = "IUdata_train_debug.json"
#
# # 获取当前文件目录
# current_directory = os.getcwd()
#
# # 拼接JSON文件的完整路径
# json_file_path = os.path.join(current_directory, json_file_name)
#
# # 读取JSON文件
# with open(json_file_path, 'r') as file:
#     data = json.load(file)
#
# # 获取JSON文件中dict的长度
# dict_length = len(data)
#
# print(f"文件'{json_file_name}'中dict的长度为: {dict_length}")

